# Figures

Curated English visual summaries used directly in the main GitHub README. These images are exported from the English presentation included in `presentation/`.

Fresh experiment plots generated by the code are written to `results/generated/plots/` and are ignored by Git by default.
